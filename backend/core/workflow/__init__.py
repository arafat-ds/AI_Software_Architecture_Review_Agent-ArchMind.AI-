"""LangGraph workflow package.

Contains:
- state/  — AnalysisState TypedDict definition and factory function
- nodes/  — Eight thin node functions (one per workflow step)
- graph/  — StateGraph construction and compiled graph singleton
"""
